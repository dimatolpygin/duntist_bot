-- Флаг авто-собранного заказа: клиент прислал файлы, но не завершил заказ
-- (не нажал «Завершить» / не ввёл имя и количество). Через 10 минут простоя
-- фоновый воркер досылает файлы в группу с пометкой и без данных клиента.
ALTER TABLE orders ADD COLUMN IF NOT EXISTS is_auto BOOLEAN NOT NULL DEFAULT false;
